/**
 * Draw Width Configuration
 * Generated by calibration script
 * Generated: 2026-02-04T20:51:16.109Z
 * Based on historical match outcome data
 */

export const DRAW_WIDTH_CONFIG = {
  'premier-league': 90, // England Premier League - Default (run calibration to optimize)
  'serie-a': 90, // Italy Serie A - Default (run calibration to optimize)
  'la-liga': 90, // Spain La Liga - Default (run calibration to optimize)
  'bundesliga': 90, // Germany Bundesliga - Default (run calibration to optimize)
  'ligue-1': 90, // France Ligue 1 - Default (run calibration to optimize)
  'global': 90 // Global fallback
};

/**
 * Map internal league codes (UK1, IT1, etc.) to calibration codes
 */
const LEAGUE_CODE_MAPPING = {
  // England
  'UK1': 'premier-league',
  'UK2': 'championship',

  // Germany
  'DE1': 'bundesliga',
  'DE2': 'bundesliga-2',

  // Italy
  'IT1': 'serie-a',
  'IT2': 'serie-b',

  // Spain
  'ES1': 'la-liga',
  'ES2': 'la-liga-2',

  // France
  'FR1': 'ligue-1',
  'FR2': 'ligue-2',

  // Netherlands
  'NL1': 'eredivisie',

  // Portugal
  'PT1': 'primeira-liga',

  // Belgium
  'BE1': 'pro-league',

  // Other major leagues can be added as needed
};

/**
 * Get optimal draw width for a league
 * @param {string} leagueCode - Internal league code (e.g., 'UK1', 'IT1', 'ES1')
 * @returns {number} - Calibrated draw width for the league
 */
export function getDrawWidth(leagueCode) {
  // If no league code provided, use global default
  if (!leagueCode) {
    return DRAW_WIDTH_CONFIG.global;
  }

  // Try direct lookup first (for calibration system codes like 'premier-league')
  if (DRAW_WIDTH_CONFIG[leagueCode]) {
    return DRAW_WIDTH_CONFIG[leagueCode];
  }

  // Try mapping from internal code (UK1 -> premier-league)
  const calibrationCode = LEAGUE_CODE_MAPPING[leagueCode];
  if (calibrationCode && DRAW_WIDTH_CONFIG[calibrationCode]) {
    return DRAW_WIDTH_CONFIG[calibrationCode];
  }

  // Fallback to global default
  return DRAW_WIDTH_CONFIG.global;
}